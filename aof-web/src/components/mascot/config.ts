/**
 * Master on/off switch for TAOTAO's visual rendering — flip back to `true`
 * to restore it. Every mascot component (ComposerMascot, TaotaoAvatar,
 * TaotaoSprite, effects) is untouched and fully functional; this just stops
 * them from rendering anything while the flag is off.
 */
export const TAOTAO_VISIBLE = false;
