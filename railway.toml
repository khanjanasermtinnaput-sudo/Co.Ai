[build]
builder = "nixpacks"
buildCommand = "cd aof-web && npm ci && npm run build"

[deploy]
startCommand = "cd aof-web && npm start -- -p $PORT"
healthcheckPath = "/api/auth/check"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
