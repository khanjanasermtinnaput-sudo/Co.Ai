services:
  - type: web
    name: coagentix
    runtime: node
    rootDir: tmap-v2
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: COAGENTIX_MASTER_KEY
        generateValue: true
      - key: COAGENTIX_MODE
        value: normal
      - key: NODE_ENV
        value: production
