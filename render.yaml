services:
  - type: web
    name: nexora-code
    runtime: node
    rootDir: tmap-v2
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NEXORA_MASTER_KEY
        generateValue: true
      - key: NEXORA_MODE
        value: normal
      - key: NODE_ENV
        value: production
